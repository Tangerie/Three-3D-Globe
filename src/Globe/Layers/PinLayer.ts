import * as THREE from "three";
import { Pin } from "../types";

export default class PinLayer extends THREE.Group {
    constructor(pins : Pin[]) {
        super();

        const hex = new THREE.Mesh()
    }
}   